import { Inter, Lato, Lusitana, Poppins } from 'next/font/google';
 
export const inter = Lato({ subsets: ['latin'], weight: "700" });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });